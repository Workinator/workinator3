Consumer
    -- KEY
    consumerId

    -- CONFIGURATION
    maxExecutorCount

Partitions
    -- KEY
    partitionKey

    -- CONFIGURATION
    maxWorkerCount
    maxIdleTimeSeconds

    -- STATUS
    hasMoreWork
    assignmentCount - how many times a worker was assigned
    lastChecked
    workerCount
    workers
        id
        insertDate
        rule