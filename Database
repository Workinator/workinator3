Consumer
    -- KEY
    consumerId

    -- CONFIGURATION
    maxExecutorCount

Partitions
    -- KEY
    partitionKey

    -- CONFIGURATION
    maxWorkerCount
    maxIdleTimeSeconds

    -- STATUS
    hasMoreWork
    lastChecked
    workerCount
    workers
        TODO